/** Bundler-resolved URL for the pinned, repository-local Vodozemac WASM artifact. */
export const VODOZEMAC_WASM_URL = new URL(
    '/crypto/k3ncrypt_vodozemac_bg.wasm',
    window.location.origin,
);
